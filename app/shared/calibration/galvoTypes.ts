export type UndistItem = {
  runTs: string;
  cam: string;
  mapXYPath: string;
};

export type GridParams = { nx: number; ny: number };
export type Range = { min: number; max: number };
export type Timing = { settleMs: number; onMs: number; offMs: number };

